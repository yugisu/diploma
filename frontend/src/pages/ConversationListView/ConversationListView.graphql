query GetConversationList {
  conversationList {
    id

    activity {
      id
      title
    }
    lastMessage {
      id
      createdByParticipant {
        id
        name
      }
      content
    }
  }
}
