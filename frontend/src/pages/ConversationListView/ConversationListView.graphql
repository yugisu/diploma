query GetConversationList {
  conversationList {
    id
    createdAt

    activity {
      id
      title
    }
    lastMessage {
      id
      createdByParticipant {
        id
        name
      }
      content
      createdAt
    }
  }
}
